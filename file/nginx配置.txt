root@iZ2zeez62n8bim3c3ya81rZ:~# sudo cat /etc/nginx/sites-available/myproject

server {
    listen 80;                 # HTTP 端口
    server_name 123.56.58.14;    # 可以换成域名

    root /home/root/myproject/frontend/dist;  # 指向 dist 目录
    index index.html;

    # 前端页面访问规则
    location / {
        try_files $uri $uri/ /index.html;
    }

    # 后端 API 代理规则（Spring Boot 监听 8080）
    location /api/ {
        proxy_pass http://localhost:8080/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        # 如果需要保持会话（与axios的withCredentials: true配合）
        proxy_set_header Cookie $http_cookie;
        proxy_cookie_path / /;
    }
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
            expires 1y;
            add_header Cache-Control "public, no-transform";
    }
    #禁用favicon.ico日志
    location = /favicon.ico {
        log_not_found off;
        access_log off;
    }
    location = /robots.txt {
        log_not_found off;                                                                     access_log off;
    }
}